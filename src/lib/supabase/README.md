# Supabase boilerplate boundary

This folder contains only generic browser-client initialisation. It does not
define a database schema, migrations, tables, policies, queries, seed data, or
domain types. Implement those during the official build window.
