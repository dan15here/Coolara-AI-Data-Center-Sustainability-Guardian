# AI boilerplate boundary

Environment-variable names are documented in `.env.example`. There is
intentionally no model call, system prompt, product-specific input, or API route
before the build window.
